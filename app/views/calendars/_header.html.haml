.header
  %nav
    .header__left
      .title
        = @calendar.name
      .user
        .user__edit
          = link_to edit_calendar_user_path(@calendar.id, current_user.id), id: 'user__edit--icon' do
            = icon('fas', 'user-edit')
          %span.pop
            ユーザー編集
      .user__name
        %span ようこそ
        = current_user.name + "  さん"